<h1>New Topic</h1>

<div class="row">
    <div class="col-md-4">
        <p>Guidlines for topics:</p>
        <ul>
            <li>Make sure the topic is appropriate.</li>
            <li>Never insult dogs.</li>
            <li>Never make me type this dumb shit again!</li>
        </ul>
    </div>
    <div calss="col-md-8">
        <%= form_for @topic do |f| %>
        <div class="form_group">
            <%= f.label :name %>
            <%= f.text_field :name, class: 'form-control', placeholder: "Topic name" %>
        </div>
        <div class="form-group">
            <%= f.label :description %>
            <%= f.text_field :description, rows: 8, class: 'form-control', placeholder: "Topic description" %>
        </div>
        <div class="form_group">
            <%= f.label :public, class: 'checkbox' do %>
            <%= f.check_box :public %> Public topic
            <% end %>
        </div>
        <%= f.submit "Save", class: 'btn btn-success' %>
        <% end %>
    </div>
</div>